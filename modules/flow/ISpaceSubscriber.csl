interface ISpaceSubscriber
{
	readonly co.IObject rootObject;
	
	void setSpace( in ca.ISpace space );
	
	bool subscribe( in int8[] bytes, in string modelName );
	
	bool onPublish( in NewObject[] newObjects, in ChangeSet[] changes );
	
};