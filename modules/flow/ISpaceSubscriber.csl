interface ISpaceSubscriber
{
	void setSpace( in ca.ISpace space );

	readonly co.IObject rootObject;
	
	void onSubscribed( in int8[] bytes, in string modelName );
	
	void onPublish( in NewObject[] newObjects, in ChangeSet[] changes );
};