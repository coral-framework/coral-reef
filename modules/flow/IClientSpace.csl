interface IClientSpace
{
	readonly co.IObject rootObject;
	ca.ISpace space;
	
	bool initializeData( in int8[] bytes, in string modelName );
	
	void registerRemoteSpaceObserver( in string serverAddress, in string serverSpaceKey );
};