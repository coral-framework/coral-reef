interface IServerNode
{
	void start( in string address );
	void stop();
	
	void update();
	
	//! makes an instance available for remote access through returned address
	int32 publishInstance( in co.IObject instance );
};